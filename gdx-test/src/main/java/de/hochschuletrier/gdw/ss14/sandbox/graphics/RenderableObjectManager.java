package de.hochschuletrier.gdw.ss14.sandbox.graphics;

/**
 * Contains all renderable objects and renders them.
 * Call addRenderableObject to add one.
 * All added objects must implement the IRenderable interface.
 * 
 * @author Milan Rüll
 *
 */
public class RenderableObjectManager {
    
    public void addRenderableObject( IRenderable newRenderable ) {
    }
    
    public void draw() {      
    }
    
    public void update( float delta ) {     
    }
}
